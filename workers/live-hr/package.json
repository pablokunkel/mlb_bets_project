{
  "name": "dingersonly-live-hr",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that polls the MLB Stats API every minute and serves today's home runs to dingersonly.cc",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create LIVE_HR_KV && wrangler kv namespace create LIVE_HR_KV --preview"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
