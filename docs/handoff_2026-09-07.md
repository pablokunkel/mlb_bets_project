# Session handoff — 2026-09-07

Bootstraps a fresh session. Read top-to-bottom. Supersedes
`docs/handoff_2026-05-26.md` for state; that doc's environment gotchas
(Python 3.14 / PowerShell, worktree DB-path trap, R2 pull) still apply.

## Current state of main

- **Commit:** see `git log origin/main -1` — the 2026-09-07 session merged
  PRs #125, #129, #130, #131, #132 and the docs PR on top of the
  `Daily update 2026-09-07` commit.
- **DB:** R2 is the source of truth (`python infra/r2_sync.py pull` from the
  MAIN checkout; from a worktree set `HR_BETS_DB` first or the pull lands in
  `.claude/worktrees/data/`). Canonical local copy at
  `C:/dev/Claude/Projects/data/hr_bets.db` was refreshed 2026-09-07 (117.7 MB,
  `pick_inputs` through 09-07, `outcomes` through 09-06).
- **Card performance:** 19.1% (141/738) 2026-06-03 → 09-06. Realistic band
  after today's fixes: 20-21%. Hindsight ceiling ~25%. The 36-40% in older
  docs is not a target.

## What shipped 2026-09-07 (all off `main`, one PR each)

| PR | what | first live effect |
|---|---|---|
| #125 | B35 empirical park factors + Sutter / LV / FoD geo rows | nightly 2026-09-08 writes `park_factors` `source='empirical_blend_v1'` rows; `[park_factors]` WARN line disappears from the picks log |
| #129 | odds capture from any US book (DK never posts the market) | `hr_prop_odds` rows with `bookmaker='betrivers'` from the 09-08 09:07 run |
| #130 | small-sample power shrink + `pick_inputs.power_sample_pa` | 09-08 board: no `power_score = 100` rows with tiny samples; `[power-guard]`-style behaviour now via the shrink |
| #131 | B36 real handedness | 09-08 board: `pick_inputs.bats` shows L/R/S; smoke probe `db_pick_inputs_bats_distribution` flips INFO → PASS |
| #132 | pre-game revalidation workflow | 16:37 / 21:07 UTC runs; `[revalidate] SUMMARY` in the log; swapped rows carry `promoted_due_to='revalidate'` / `status_description='revalidate …'` |

## What to check tomorrow (2026-09-08) — in order

1. `nightly-refresh` log: `[BATS] 500 handedness resolved`, `[BATS] N/N leftover`,
   and step 8 (park factors) writing blended rows without a migration error.
2. `daily-picks` log: no `[park_factors] WARNING … fell through`, the
   `[odds] picks priced per book:` line, and `Persisted N pick_inputs rows`.
3. `revalidate-picks` (two runs): `REVALIDATE_CHANGED=…`; on a change, a
   `Revalidate picks 2026-09-08` commit on main and the site card updated.
4. `python -m tests.smoke` from the main checkout after an R2 pull:
   expect 138+ PASS, the bats probe PASS, park-venue probe PASS.

## Known state that looks broken but isn't

Everything on CLAUDE.md's false-alarm list still applies, plus:

- `pick_inputs.power_sample_pa` NULL on every row before 2026-09-08 — by
  design; `score_power` applies no shrink when it is NULL, which is how those
  rows were scored.
- `pick_inputs.bats = 'R'` on every row before 2026-09-08 — pre-B36 data;
  backtests on those rows cannot see platoon. Do not backfill it into
  history (it would make old rows look like they were scored with real
  handedness).
- `daily_picks.is_likely_out = 1` + `status_description LIKE 'revalidate%'`
  — a pick removed by the pre-game re-check, not an IL flag.
- `hr_prop_odds` shows BetRivers, not DraftKings — that is all the API
  serves for this market.

## Where the next hit-rate points are (see `docs/backtest_2026-09-07.md` §4)

1. Prior-season HR as a talent prior (through `refit_weights.py`'s OOS gate).
2. As-of-date real Statcast `brl_pa` nightly, then retire synthetic barrel.
3. Pitcher hand-split HR/9 + a real platoon term in matchup v2.
4. Expected-PA multiplier by batting order (replaces B15).

Do NOT spend a session on another weight grid: the backtest shows every
blend from power-only to FREE within 14-20%, all inside noise.

## Environment reminders (unchanged)

- Python 3.14 + PowerShell + Windows. Bash heredocs with `'` inside break
  under this tool chain — write scripts to a file and run them.
- `Remove-Item` is sandbox-blocked; use `python -c "import shutil; …"`.
- `git commit -F -` with a heredoc works; here-strings do not.
- Scratch DB copies for e2e runs: put them in
  `C:/dev/Claude/Projects/data/` (so `CACHE_DIR` stays warm) and delete
  after (`hr_bets_scratch.db` from this session was removed).
